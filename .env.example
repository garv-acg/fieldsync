# Supabase project credentials (required)
# Found at: supabase.com → your project → Settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Web Push VAPID private key (required for push notifications)
# Generate with: npx web-push generate-vapid-keys
VAPID_PRIVATE_KEY=
